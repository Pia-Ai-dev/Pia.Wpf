<UserControl x:Class="Pia.Views.Controls.RecordingIndicator"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  mc:Ignorable="d"
  d:DesignHeight="200" d:DesignWidth="200">
  <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
    <Grid HorizontalAlignment="Center">
      <Ellipse x:Name="IndicatorEllipse"
               Width="{Binding EllipseSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
               Height="{Binding EllipseSize, RelativeSource={RelativeSource AncestorType=UserControl}}"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               RenderTransformOrigin="0.5,0.5">
        <Ellipse.Fill>
          <SolidColorBrush Color="{DynamicResource SystemAccentColorSecondary}" />
        </Ellipse.Fill>
        <Ellipse.Effect>
          <DropShadowEffect x:Name="IndicatorGlow"
                            ShadowDepth="0"
                            BlurRadius="8"
                            Opacity="0.3" />
        </Ellipse.Effect>
        <Ellipse.RenderTransform>
          <ScaleTransform x:Name="IndicatorScale" ScaleX="1" ScaleY="1" />
        </Ellipse.RenderTransform>
      </Ellipse>
    </Grid>

    <TextBlock x:Name="DurationTextBlock"
               Text="00:00"
               FontSize="24"
               FontWeight="Bold"
               HorizontalAlignment="Center"
               Margin="0,15,0,0"
               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
               Visibility="Collapsed" />

    <TextBlock Text="{Binding StatusText, RelativeSource={RelativeSource AncestorType=UserControl}}"
               FontSize="20"
               FontWeight="SemiBold"
               HorizontalAlignment="Center"
               Margin="0,8,0,0"
               Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
  </StackPanel>
</UserControl>
