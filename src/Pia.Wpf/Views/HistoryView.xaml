<UserControl x:Class="Pia.Views.HistoryView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
              xmlns:local="clr-namespace:Pia.Views"
              xmlns:loc="clr-namespace:Pia.Localization"
              xmlns:nav="clr-namespace:Pia.Navigation"
              mc:Ignorable="d"
              d:DesignHeight="600" d:DesignWidth="900"
              nav:ViewModelLocator.AutoWireViewModel="True">
  <UserControl.InputBindings>
    <KeyBinding Key="F" Modifiers="Control" Command="{Binding FocusSearchCommand}" />
    <KeyBinding Key="Delete" Command="{Binding DeleteSessionCommand}" />
    <KeyBinding Key="Enter" Command="{Binding ViewDetailCommand}" />
  </UserControl.InputBindings>

  <Grid Background="{DynamicResource PrimaryBackgroundBrush}"
        Margin="20 45 20 20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0"
               Text="{loc:Str History_Title}"
               Style="{StaticResource H1TextStyle}"
               Margin="20,20,20,10" />

    <Border Grid.Row="1"
            Background="{DynamicResource SecondaryBackgroundBrush}"
            BorderBrush="{DynamicResource CardBorderBrush}"
            BorderThickness="1"
            CornerRadius="4"
            Margin="20,0,20,10"
            Padding="10">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <DatePicker Grid.Column="0"
                   SelectedDate="{Binding FilterStartDate}"
                   Width="180"
                   Margin="0,0,4,0" />

        <TextBlock Grid.Column="1"
                  Text="{loc:Str History_DateRangeTo}"
                  VerticalAlignment="Center"
                  Margin="0,0,4,0" />

        <DatePicker Grid.Column="2"
                   SelectedDate="{Binding FilterEndDate}"
                   Width="180"
                   Margin="0,0,8,0" />

        <Grid Grid.Column="3"
              Width="160"
              Margin="0,0,8,0">
          <ComboBox ItemsSource="{Binding Templates}"
                   SelectedValuePath="Id"
                   SelectedValue="{Binding SelectedTemplateId}">
            <ComboBox.ItemTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding Name}" />
              </DataTemplate>
            </ComboBox.ItemTemplate>
          </ComboBox>
          <TextBlock Text="{loc:Str History_TemplateFilter_Placeholder}"
                     IsHitTestVisible="False"
                     VerticalAlignment="Center"
                     Margin="8,0,0,0"
                     Foreground="{DynamicResource TextPlaceholderColorBrush}">
            <TextBlock.Style>
              <Style TargetType="TextBlock">
                <Setter Property="Visibility" Value="Collapsed" />
                <Style.Triggers>
                  <DataTrigger Binding="{Binding SelectedTemplateId}" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Visible" />
                  </DataTrigger>
                </Style.Triggers>
              </Style>
            </TextBlock.Style>
          </TextBlock>
        </Grid>

        <ui:TextBox Grid.Column="4"
                    Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                    PlaceholderText="{loc:Str History_SearchPlaceholder}"
                    PlaceholderEnabled="True"
                    ClearButtonEnabled="False"
                    MinWidth="100"
                    Margin="0,0,8,0" />

        <ui:Button Grid.Column="5"
                   Command="{Binding RefreshCommand}"
                   Appearance="Secondary"
                   Width="36"
                   Height="36"
                   Padding="0"
                   Margin="0,0,4,0"
                   ToolTip="{loc:Str History_Refresh}">
          <Grid>
            <ui:SymbolIcon Symbol="ArrowSync24" FontSize="16"
                           Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}" />
            <ui:ProgressRing IsIndeterminate="True"
                             Width="16" Height="16"
                             Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />
          </Grid>
        </ui:Button>

        <ui:Button Grid.Column="6"
                   Command="{Binding ClearFilterCommand}"
                   Appearance="Secondary"
                   Width="36"
                   Height="36"
                   Padding="0"
                   ToolTip="{loc:Str History_ClearFilters}">
          <ui:SymbolIcon Symbol="FilterDismiss24" FontSize="16" />
        </ui:Button>
      </Grid>
    </Border>

    <Grid Grid.Row="2" Margin="20,0,20,10">
      <DataGrid x:Name="SessionsDataGrid"
                ItemsSource="{Binding Sessions}"
                SelectedItem="{Binding SelectedSession}"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                CanUserReorderColumns="False"
                SelectionMode="Single"
                GridLinesVisibility="Horizontal"
                HeadersVisibility="Column"
                Background="{DynamicResource PrimaryBackgroundBrush}"
                BorderBrush="{DynamicResource CardBorderBrush}"
                BorderThickness="1"
                AlternatingRowBackground="{DynamicResource SecondaryBackgroundBrush}"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling"
                EnableRowVirtualization="True"
                RowHeaderWidth="0">
        <DataGrid.Columns>
          <DataGridTextColumn Header="{loc:Str History_DateColumn}"
                              Binding="{Binding CreatedAt, StringFormat='{}{0:g}'}"
                              Width="140">
          </DataGridTextColumn>
          <DataGridTextColumn Header="{loc:Str History_TemplateColumn}"
                              Binding="{Binding TemplateName}"
                              Width="180">
          </DataGridTextColumn>
          <DataGridTextColumn Header="{loc:Str History_ProviderColumn}"
                              Binding="{Binding ProviderName}"
                              Width="180">
          </DataGridTextColumn>
          <DataGridTextColumn Header="{loc:Str History_PreviewColumn}"
                              Binding="{Binding OriginalText}"
                              Width="250">
          </DataGridTextColumn>
          <DataGridTemplateColumn Header="{loc:Str History_ActionsColumn}" Width="Auto">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <StackPanel Orientation="Horizontal">
                  <Button Command="{Binding DataContext.ViewDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          Width="40"
                          Height="40"
                          Margin="0,0,5,0">
                    <ui:SymbolIcon Symbol="Eye24" FontSize="16"/>
                    <Button.ToolTip>
                      <ToolTip Content="{loc:Str History_View}"/>
                    </Button.ToolTip>
                  </Button>
                  <Button Command="{Binding DataContext.CopyOptimizedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          Width="40"
                          Height="40"
                          Margin="0,0,5,0">
                    <ui:SymbolIcon Symbol="Copy24" FontSize="16"/>
                    <Button.ToolTip>
                      <ToolTip Content="{loc:Str History_Copy}"/>
                    </Button.ToolTip>
                  </Button>
                  <Button Command="{Binding DataContext.DeleteSessionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          Width="40"
                          Height="40">
                    <ui:SymbolIcon Symbol="Delete24" FontSize="16"/>
                    <Button.ToolTip>
                      <ToolTip Content="{loc:Str History_Delete}"/>
                    </Button.ToolTip>
                  </Button>
                </StackPanel>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
        </DataGrid.Columns>
          <DataGrid.ContextMenu>
            <ContextMenu Background="{DynamicResource SecondaryBackgroundBrush}">
              <MenuItem Header="{loc:Str History_ViewDetail}" Command="{Binding DataContext.ViewDetailCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <MenuItem.Icon>
                  <ui:SymbolIcon Symbol="Eye24" FontSize="16"/>
                </MenuItem.Icon>
              </MenuItem>
              <MenuItem Header="{loc:Str History_CopyOriginal}" Command="{Binding DataContext.CopyOriginalCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <MenuItem.Icon>
                  <ui:SymbolIcon Symbol="Copy24" FontSize="16"/>
                </MenuItem.Icon>
              </MenuItem>
              <MenuItem Header="{loc:Str History_CopyOptimized}" Command="{Binding DataContext.CopyOptimizedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <MenuItem.Icon>
                <ui:SymbolIcon Symbol="Copy24" FontSize="16"/>
                </MenuItem.Icon>
              </MenuItem>
              <Separator />
              <MenuItem Header="{loc:Str History_Delete}" Command="{Binding DataContext.DeleteSessionCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
                <MenuItem.Icon>
                  <ui:SymbolIcon Symbol="Delete24" FontSize="16"/>
                </MenuItem.Icon>
              </MenuItem>
            </ContextMenu>
          </DataGrid.ContextMenu>
        <DataGrid.InputBindings>
          <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding ViewDetailCommand}" />
        </DataGrid.InputBindings>
      </DataGrid>

      <StackPanel Grid.Row="2"
                 Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                 Background="{DynamicResource PrimaryBackgroundBrush}"
                 Opacity="0.8">
        <TextBlock Text="{loc:Str History_Loading}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="16"
                   Margin="20" />
      </StackPanel>

      <TextBlock Grid.Row="2"
                 Text="{loc:Str History_NoSessions}"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 FontSize="16"
                 Visibility="{Binding Sessions.Count, Converter={StaticResource ZeroToVisibilityConverter}}" />
    </Grid>

    <StackPanel Grid.Row="3"
               Orientation="Horizontal"
               HorizontalAlignment="Center"
               Margin="20">
      <Button Command="{Binding LoadMoreCommand}"
              Width="150"
              Margin="5"
              Visibility="{Binding IsLoading, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
        <StackPanel Orientation="Horizontal">
          <ui:SymbolIcon Symbol="ChevronDown24" FontSize="16" Margin="0,0,8,0"/>
          <TextBlock Text="{loc:Str History_LoadMore}"/>
        </StackPanel>
      </Button>
      <TextBlock Text="{Binding TotalCount, StringFormat='Showing {0} sessions'}"
                 VerticalAlignment="Center"
                 Margin="10" />
    </StackPanel>
  </Grid>
</UserControl>

