<UserControl x:Class="Pia.Views.RemindersView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
              xmlns:local="clr-namespace:Pia.Views"
              xmlns:loc="clr-namespace:Pia.Localization"
              xmlns:nav="clr-namespace:Pia.Navigation"
              mc:Ignorable="d"
              d:DesignHeight="600" d:DesignWidth="900"
              nav:ViewModelLocator.AutoWireViewModel="True">

  <Grid Background="{DynamicResource PrimaryBackgroundBrush}"
        Margin="20 45 20 20">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0"
               Text="{loc:Str Reminders_Title}"
               Style="{StaticResource H1TextStyle}"
               Margin="20,20,20,10" />

    <Border Grid.Row="1"
            Background="{DynamicResource SecondaryBackgroundBrush}"
            BorderBrush="{DynamicResource CardBorderBrush}"
            BorderThickness="1"
            CornerRadius="4"
            Margin="20,0,20,10"
            Padding="15">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto" />
          <ColumnDefinition Width="*" />
          <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <ComboBox Grid.Column="0"
                  ItemsSource="{Binding StatusFilters}"
                  SelectedItem="{Binding StatusFilter}"
                  Width="150" />

        <Button Grid.Column="2"
                Command="{Binding RefreshCommand}"
                Width="110">
          <StackPanel Orientation="Horizontal">
            <ui:SymbolIcon Symbol="ArrowSync24" FontSize="16" Margin="0,0,8,0"/>
            <TextBlock Text="{loc:Str Reminders_Refresh}"/>
          </StackPanel>
        </Button>
      </Grid>
    </Border>

    <Grid Grid.Row="2" Margin="20,0,20,10">
      <DataGrid x:Name="RemindersDataGrid"
                ItemsSource="{Binding Reminders}"
                SelectedItem="{Binding SelectedReminder}"
                AutoGenerateColumns="False"
                IsReadOnly="True"
                CanUserAddRows="False"
                CanUserDeleteRows="False"
                CanUserReorderColumns="False"
                SelectionMode="Single"
                GridLinesVisibility="Horizontal"
                HeadersVisibility="Column"
                Background="{DynamicResource PrimaryBackgroundBrush}"
                BorderBrush="{DynamicResource CardBorderBrush}"
                BorderThickness="1"
                AlternatingRowBackground="{DynamicResource SecondaryBackgroundBrush}"
                VirtualizingPanel.IsVirtualizing="True"
                VirtualizingPanel.VirtualizationMode="Recycling"
                EnableRowVirtualization="True"
                RowHeaderWidth="0">
        <DataGrid.Columns>
          <DataGridTextColumn Header="{loc:Str Reminders_DescriptionColumn}"
                              Binding="{Binding Description}"
                              Width="*" />
          <DataGridTextColumn Header="{loc:Str Reminders_RecurrenceColumn}"
                              Binding="{Binding Recurrence}"
                              Width="90" />
          <DataGridTextColumn Header="{loc:Str Reminders_TimeColumn}"
                              Binding="{Binding TimeOfDay, StringFormat='{}{0:t}'}"
                              Width="70" />
          <DataGridTextColumn Header="{loc:Str Reminders_NextFireColumn}"
                              Binding="{Binding NextFireAt, StringFormat='{}{0:g}'}"
                              Width="140" />
          <DataGridTextColumn Header="{loc:Str Reminders_StatusColumn}"
                              Binding="{Binding Status}"
                              Width="90" />
          <DataGridTemplateColumn Header="{loc:Str Reminders_ActionsColumn}" Width="Auto">
            <DataGridTemplateColumn.CellTemplate>
              <DataTemplate>
                <StackPanel Orientation="Horizontal">
                  <Button Command="{Binding DataContext.ToggleEnableCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          Width="40"
                          Height="40"
                          Margin="0,0,5,0">
                    <ui:SymbolIcon Symbol="ToggleRight24" FontSize="16"/>
                    <Button.ToolTip>
                      <ToolTip Content="{loc:Str Reminders_ToggleEnable}"/>
                    </Button.ToolTip>
                  </Button>
                  <Button Command="{Binding DataContext.SnoozeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          Width="40"
                          Height="40"
                          Margin="0,0,5,0">
                    <ui:SymbolIcon Symbol="ClockAlarm24" FontSize="16"/>
                    <Button.ToolTip>
                      <ToolTip Content="{loc:Str Reminders_Snooze}"/>
                    </Button.ToolTip>
                  </Button>
                  <Button Command="{Binding DataContext.DismissCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          Width="40"
                          Height="40"
                          Margin="0,0,5,0">
                    <ui:SymbolIcon Symbol="Checkmark24" FontSize="16"/>
                    <Button.ToolTip>
                      <ToolTip Content="{loc:Str Reminders_Dismiss}"/>
                    </Button.ToolTip>
                  </Button>
                  <Button Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                          Width="40"
                          Height="40">
                    <ui:SymbolIcon Symbol="Delete24" FontSize="16"/>
                    <Button.ToolTip>
                      <ToolTip Content="{loc:Str Reminders_Delete}"/>
                    </Button.ToolTip>
                  </Button>
                </StackPanel>
              </DataTemplate>
            </DataGridTemplateColumn.CellTemplate>
          </DataGridTemplateColumn>
        </DataGrid.Columns>
        <DataGrid.ContextMenu>
          <ContextMenu Background="{DynamicResource SecondaryBackgroundBrush}">
            <MenuItem Header="{loc:Str Reminders_ToggleEnable}" Command="{Binding DataContext.ToggleEnableCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
              <MenuItem.Icon>
                <ui:SymbolIcon Symbol="ToggleRight24" FontSize="16"/>
              </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="{loc:Str Reminders_Snooze}" Command="{Binding DataContext.SnoozeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
              <MenuItem.Icon>
                <ui:SymbolIcon Symbol="ClockAlarm24" FontSize="16"/>
              </MenuItem.Icon>
            </MenuItem>
            <MenuItem Header="{loc:Str Reminders_Dismiss}" Command="{Binding DataContext.DismissCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
              <MenuItem.Icon>
                <ui:SymbolIcon Symbol="Checkmark24" FontSize="16"/>
              </MenuItem.Icon>
            </MenuItem>
            <Separator />
            <MenuItem Header="{loc:Str Reminders_Delete}" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}">
              <MenuItem.Icon>
                <ui:SymbolIcon Symbol="Delete24" FontSize="16"/>
              </MenuItem.Icon>
            </MenuItem>
          </ContextMenu>
        </DataGrid.ContextMenu>
      </DataGrid>

      <StackPanel Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"
                  Background="{DynamicResource PrimaryBackgroundBrush}"
                  Opacity="0.8">
        <TextBlock Text="{loc:Str Reminders_Loading}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="16"
                   Margin="20" />
      </StackPanel>

      <TextBlock Text="{loc:Str Reminders_EmptyState}"
                 HorizontalAlignment="Center"
                 VerticalAlignment="Center"
                 FontSize="16"
                 Visibility="{Binding Reminders.Count, Converter={StaticResource ZeroToVisibilityConverter}}" />
    </Grid>
  </Grid>
</UserControl>
