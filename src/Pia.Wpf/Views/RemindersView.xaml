<UserControl x:Class="Pia.Views.RemindersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:rem="clr-namespace:Pia.Controls.Reminders"
             xmlns:loc="clr-namespace:Pia.Localization"
             xmlns:nav="clr-namespace:Pia.Navigation"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1100"
             nav:ViewModelLocator.AutoWireViewModel="True">
  <Grid Background="Transparent">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <rem:PiaRemindersHeader Grid.Row="0" Margin="24,20,24,14"/>

    <rem:PiaRemindersFilterBar Grid.Row="1"
                               Margin="24,0,24,14"
                               ActiveFilter="{Binding StatusFilter, Mode=TwoWay}"/>

    <ScrollViewer Grid.Row="2"
                  Margin="24,0,24,14"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
      <Grid>
        <ui:ProgressRing IsIndeterminate="True"
                         Width="40" Height="40"
                         VerticalAlignment="Top"
                         HorizontalAlignment="Center"
                         Margin="0,60,0,0"
                         Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <StackPanel HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Margin="0,60,0,0"
                    Visibility="{Binding ReminderGroups.Count, Converter={StaticResource ZeroToVisibilityConverter}}">
          <ui:SymbolIcon Symbol="Alert24"
                         FontSize="48"
                         HorizontalAlignment="Center"
                         Margin="0,0,0,16"
                         Foreground="{DynamicResource TextSubtleBrush}"/>
          <TextBlock Text="{loc:Str Reminders_EmptyState}"
                     FontSize="14"
                     HorizontalAlignment="Center"
                     TextAlignment="Center"
                     MaxWidth="360"
                     TextWrapping="Wrap"
                     Foreground="{DynamicResource TextMutedBrush}"/>
        </StackPanel>

        <ItemsControl ItemsSource="{Binding ReminderGroups}">
          <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
              <StackPanel Orientation="Vertical"/>
            </ItemsPanelTemplate>
          </ItemsControl.ItemsPanel>
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <rem:PiaReminderGroupCard Margin="0,0,0,10"/>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
      </Grid>
    </ScrollViewer>
  </Grid>
</UserControl>
