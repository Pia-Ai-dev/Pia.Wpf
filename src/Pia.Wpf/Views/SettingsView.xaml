<UserControl x:Class="Pia.Views.SettingsView"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:local="clr-namespace:Pia.Views"
              xmlns:localSettings="clr-namespace:Pia.Views.SettingsViews"
              xmlns:nav="clr-namespace:Pia.Navigation"
              xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
              xmlns:loc="clr-namespace:Pia.Localization"
              mc:Ignorable="d"
              d:DesignHeight="450" d:DesignWidth="800"
              nav:ViewModelLocator.AutoWireViewModel="True">
  <Grid Background="{DynamicResource PrimaryBackgroundBrush}" Margin="20 45 20 20">
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <TabControl Grid.Row="0" SelectedIndex="{Binding SelectedTabIndex}" Margin="0,0,0,0">
      <TabItem Header="{loc:Str Settings_Tab_Providers}">
        <localSettings:ProvidersView/>
      </TabItem>
      <TabItem Header="{loc:Str Settings_Tab_Templates}">
        <localSettings:TemplatesView/>
      </TabItem>
      <TabItem Header="{loc:Str Settings_Tab_General}">
        <localSettings:GeneralView/>
      </TabItem>
      <TabItem Header="{loc:Str Settings_Tab_TextToSpeech}">
        <localSettings:TtsView/>
      </TabItem>
      <TabItem Header="{loc:Str Settings_Tab_CloudSync}">
        <localSettings:SyncView/>
      </TabItem>
      <TabItem Header="{loc:Str Settings_Tab_Privacy}">
        <localSettings:PrivacyView/>
      </TabItem>
    </TabControl>
  </Grid>
 </UserControl>
