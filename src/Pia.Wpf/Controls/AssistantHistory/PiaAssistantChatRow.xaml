<UserControl x:Class="Pia.Controls.AssistantHistory.PiaAssistantChatRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:ah="clr-namespace:Pia.Controls.AssistantHistory">
  <Border x:Name="Root"
          CornerRadius="8"
          Padding="28,8,14,8"
          Background="Transparent"
          Cursor="Hand">
    <Border.Style>
      <Style TargetType="Border">
        <Style.Triggers>
          <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                       Value="True">
            <Setter Property="Background" Value="{DynamicResource PiaAccentSoftBrush}"/>
          </DataTrigger>
          <Trigger Property="IsMouseOver" Value="True">
            <Setter Property="Background" Value="{DynamicResource SurfaceMutedBrush}"/>
          </Trigger>
        </Style.Triggers>
      </Style>
    </Border.Style>

    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>

      <ah:PiaAssistantChatRowContent Grid.Column="0"/>

      <!-- Live state badge (history-only wrapper carries AssistantChatRowViewModel
           with an observable State; hidden when Idle). -->
      <Border Grid.Column="1"
              CornerRadius="9"
              Padding="6,2"
              Margin="8,0,0,0"
              VerticalAlignment="Center"
              Background="{Binding State, Converter={StaticResource ChatStateToBrushBg}}"
              Visibility="{Binding State, Converter={StaticResource ChatStateIsBadgeVisibleConverter}}">
        <StackPanel Orientation="Horizontal">
          <ui:SymbolIcon Symbol="{Binding State, Converter={StaticResource ChatStateToGlyphConverter}}"
                         FontSize="11"
                         VerticalAlignment="Center"
                         Foreground="{Binding State, Converter={StaticResource ChatStateToBrushFg}}"/>
          <TextBlock Text="{Binding State, Converter={StaticResource ChatStateToLabelConverter}}"
                     FontSize="10"
                     FontWeight="SemiBold"
                     Margin="4,0,0,0"
                     VerticalAlignment="Center"
                     Foreground="{Binding State, Converter={StaticResource ChatStateToBrushFg}}"/>
        </StackPanel>
      </Border>
    </Grid>
  </Border>
</UserControl>
