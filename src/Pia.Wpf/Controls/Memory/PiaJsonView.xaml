<UserControl x:Class="Pia.Controls.Memory.PiaJsonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:loc="clr-namespace:Pia.Localization"
             xmlns:views="clr-namespace:Pia.Views"
             x:Name="Root">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <!-- Read-only highlighted view -->
    <Border Grid.Row="0"
            Background="{DynamicResource SurfaceSunkBrush}"
            BorderBrush="{DynamicResource BorderBrush_}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="12,10"
            Visibility="{Binding DataContext.IsEditing,
                                 RelativeSource={RelativeSource AncestorType=views:MemoryView},
                                 Converter={StaticResource InverseBooleanToVisibilityConverter}}">
      <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
        <TextBlock x:Name="JsonHost"
                   FontFamily="{DynamicResource PiaMonoFont}"
                   FontSize="12.5"
                   Foreground="{DynamicResource TextDefaultBrush}"
                   TextWrapping="NoWrap"/>
      </ScrollViewer>
    </Border>

    <!-- Editable view -->
    <Border Grid.Row="0"
            Background="{DynamicResource SurfaceSunkBrush}"
            BorderBrush="{DynamicResource PiaAccentBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="0"
            Visibility="{Binding DataContext.IsEditing,
                                 RelativeSource={RelativeSource AncestorType=views:MemoryView},
                                 Converter={StaticResource BooleanToVisibilityConverter}}">
      <TextBox Text="{Binding DataContext.EditingData,
                              RelativeSource={RelativeSource AncestorType=views:MemoryView},
                              UpdateSourceTrigger=PropertyChanged}"
               AcceptsReturn="True"
               AcceptsTab="True"
               TextWrapping="NoWrap"
               VerticalScrollBarVisibility="Auto"
               HorizontalScrollBarVisibility="Auto"
               FontFamily="{DynamicResource PiaMonoFont}"
               FontSize="12.5"
               Background="Transparent"
               BorderThickness="0"
               Padding="12,10"
               Foreground="{DynamicResource TextDefaultBrush}"/>
    </Border>

    <!-- Edit action row -->
    <StackPanel Grid.Row="1"
                Orientation="Horizontal"
                HorizontalAlignment="Right"
                Margin="0,10,0,0"
                Visibility="{Binding DataContext.IsEditing,
                                     RelativeSource={RelativeSource AncestorType=views:MemoryView},
                                     Converter={StaticResource BooleanToVisibilityConverter}}">
      <ui:Button Appearance="Secondary"
                 Margin="0,0,8,0"
                 Command="{Binding DataContext.CancelEditCommand,
                                   RelativeSource={RelativeSource AncestorType=views:MemoryView}}">
        <TextBlock Text="{loc:Str Memory_Cancel}"/>
      </ui:Button>
      <ui:Button Appearance="Primary"
                 Command="{Binding DataContext.SaveEditCommand,
                                   RelativeSource={RelativeSource AncestorType=views:MemoryView}}">
        <StackPanel Orientation="Horizontal">
          <ui:SymbolIcon Symbol="Save24" FontSize="16" Margin="0,0,6,0"/>
          <TextBlock Text="{loc:Str Memory_Save}"/>
        </StackPanel>
      </ui:Button>
    </StackPanel>

    <!-- Source strip -->
    <Border Grid.Row="2"
            Margin="0,12,0,0"
            Background="{DynamicResource PiaAccentSoftBrush}"
            CornerRadius="8"
            Padding="10,8"
            Visibility="{Binding SourceLabel, Converter={StaticResource NullToVisibilityConverter}}">
      <StackPanel Orientation="Horizontal">
        <ui:SymbolIcon Symbol="ChatBubblesQuestion20"
                       Foreground="{DynamicResource PiaAccentBrush}"
                       Margin="0,1,8,0"/>
        <TextBlock VerticalAlignment="Center" FontSize="12">
          <Run Text="{loc:Str Memory_SourcePrefix}" FontWeight="SemiBold"/>
          <Run Text=" "/>
          <Run Text="{Binding SourceLabel, Mode=OneWay}" Foreground="{DynamicResource TextMutedBrush}"/>
          <Run Text="  "/>
          <Hyperlink Foreground="{DynamicResource PiaAccentBrush}"
                     TextDecorations="None"
                     Command="{Binding DataContext.OpenSourceCommand, RelativeSource={RelativeSource AncestorType=views:MemoryView}}"
                     CommandParameter="{Binding}">
            <Run Text="{loc:Str Memory_OpenConversation}"/>
          </Hyperlink>
        </TextBlock>
      </StackPanel>
    </Border>
  </Grid>
</UserControl>
