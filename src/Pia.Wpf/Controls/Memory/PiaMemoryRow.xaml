<UserControl x:Class="Pia.Controls.Memory.PiaMemoryRow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:behaviors="clr-namespace:Pia.Behaviors"
             xmlns:mem="clr-namespace:Pia.Controls.Memory">
  <Border x:Name="Root"
          CornerRadius="8"
          Padding="28,8,14,8"
          Background="Transparent"
          Cursor="Hand">
    <Border.Style>
      <Style TargetType="Border">
        <Style.Triggers>
          <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                       Value="True">
            <Setter Property="Background" Value="{DynamicResource PiaAccentSoftBrush}"/>
          </DataTrigger>
          <Trigger Property="IsMouseOver" Value="True">
            <Setter Property="Background" Value="{DynamicResource SurfaceMutedBrush}"/>
          </Trigger>
        </Style.Triggers>
      </Style>
    </Border.Style>

    <!-- No per-row date: VaultMemoryItem.Updated is the DOCUMENT-level frontmatter timestamp, so showing
         it on every section row of a structured doc (contacts.md with N people) would misrepresent it as
         per-item recency. The group card header carries the document timestamp at the right granularity. -->
    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>

      <StackPanel Grid.Column="0">
        <TextBlock behaviors:EmojiTextBehavior.Text="{Binding Title}"
                   FontSize="13"
                   Foreground="{DynamicResource TextDefaultBrush}"
                   TextTrimming="CharacterEllipsis"/>
        <TextBlock behaviors:EmojiTextBehavior.Text="{Binding Preview}"
                   FontSize="11.5"
                   Margin="0,1,0,0"
                   Foreground="{DynamicResource TextSubtleBrush}"
                   TextTrimming="CharacterEllipsis"/>
      </StackPanel>

      <mem:PiaTypeChip Grid.Column="1"
                       Type="{Binding TypeKind}"
                       Margin="10,0"
                       VerticalAlignment="Center"/>
    </Grid>
  </Border>
</UserControl>
