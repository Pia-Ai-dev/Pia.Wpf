<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- Shown by the edit dialogs while the form is invalid, in place of the Save button the
       ContentDialog template hides when it is disabled. -->
  <Style x:Key="PiaRequiredHintStyle" TargetType="Border">
    <Setter Property="Background" Value="{DynamicResource PiaAccentSoftBrush}" />
    <Setter Property="CornerRadius" Value="6" />
    <Setter Property="Padding" Value="12,8" />
    <Setter Property="Margin" Value="0,0,0,16" />
  </Style>

  <!-- Primary Button Style -->
  <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
    <Setter Property="Padding" Value="16,8" />
    <Setter Property="MinWidth" Value="80" />
  </Style>

  <!-- Secondary Button Style -->
  <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
    <Setter Property="Padding" Value="16,8" />
    <Setter Property="MinWidth" Value="80" />
  </Style>

  <!-- Icon Button Style -->
  <Style x:Key="IconButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
    <Setter Property="Padding" Value="8" />
    <Setter Property="MinWidth" Value="32" />
    <Setter Property="MinHeight" Value="32" />
  </Style>

  <!-- TextBox Style -->
  <Style x:Key="DefaultTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
    <Setter Property="Padding" Value="8,6" />
  </Style>

  <!-- Multiline TextBox Style -->
  <Style x:Key="MultilineTextBoxStyle" TargetType="TextBox" BasedOn="{StaticResource DefaultTextBoxStyle}">
    <Setter Property="AcceptsReturn" Value="True" />
    <Setter Property="TextWrapping" Value="Wrap" />
    <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
    <Setter Property="MinHeight" Value="100" />
  </Style>

  <!-- ComboBox Style -->
  <Style x:Key="DefaultComboBoxStyle" TargetType="ComboBox" BasedOn="{StaticResource {x:Type ComboBox}}">
    <Setter Property="Padding" Value="8,6" />
    <Setter Property="MinWidth" Value="120" />
  </Style>

  <!-- Settings inner TabControl style (segmented pill tabs). -->
  <Style x:Key="SettingsInnerTabControlStyle" TargetType="TabControl">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="BorderThickness" Value="0"/>
    <Setter Property="Padding" Value="0"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TabControl">
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto"/>
              <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0"
                    Background="{DynamicResource SurfaceMutedBrush}"
                    BorderBrush="{DynamicResource BorderBrush_}"
                    BorderThickness="1"
                    CornerRadius="9"
                    Padding="3"
                    HorizontalAlignment="Left"
                    Margin="0,0,0,16">
              <TabPanel IsItemsHost="True"/>
            </Border>
            <!-- The part name is load-bearing: TabItemAutomationPeer exposes the selected tab's
                 content only through TabControl.SelectedContentPresenter, which is null unless the
                 template names this presenter PART_SelectedContentHost. -->
            <ContentPresenter x:Name="PART_SelectedContentHost"
                              Grid.Row="1"
                              ContentSource="SelectedContent"/>
          </Grid>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Settings inner TabItem style (pill tab header). Matches PiaSegmentedButton. -->
  <Style x:Key="SettingsInnerTabItemStyle" TargetType="TabItem">
    <Setter Property="Background" Value="Transparent"/>
    <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}"/>
    <Setter Property="Padding" Value="14,6"/>
    <Setter Property="Margin" Value="0"/>
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="TabItem">
          <!-- Hand cursor lives on the template Border, not the TabItem: Cursor is an
               inheritable DP, so on the TabItem itself it leaks into the tab content. -->
          <Border x:Name="Bd"
                  Background="{TemplateBinding Background}"
                  Cursor="Hand"
                  CornerRadius="6"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter x:Name="ContentSite"
                              ContentSource="Header"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              TextBlock.FontSize="12"
                              TextBlock.FontWeight="Medium"
                              TextBlock.Foreground="{TemplateBinding Foreground}"/>
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{DynamicResource PiaAccentBrush}"/>
              <Setter Property="Foreground" Value="White"/>
              <Setter TargetName="Bd" Property="Effect">
                <Setter.Value>
                  <DropShadowEffect Color="#1E2A4A" Opacity="0.18"
                                    BlurRadius="6" ShadowDepth="1"/>
                </Setter.Value>
              </Setter>
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <!-- Fade-in animation for a ContentPresenter whose Content binding signals
       NotifyOnTargetUpdated=True. Used for view transitions in the main shell. -->
  <Style x:Key="NavigationFadeContentPresenterStyle" TargetType="ContentPresenter">
    <Style.Triggers>
      <EventTrigger RoutedEvent="Binding.TargetUpdated">
        <BeginStoryboard>
          <Storyboard>
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                             From="0"
                             To="1"
                             Duration="0:0:0.2">
              <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
              </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
          </Storyboard>
        </BeginStoryboard>
      </EventTrigger>
    </Style.Triggers>
  </Style>

  <!-- Busy-glyph rotation. The target must carry its own INLINE RotateTransform: a transform
       handed in from a Style setter is sealed and throws when animated. -->
  <Storyboard x:Key="PiaSpinForever" RepeatBehavior="Forever">
    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                     From="0"
                     To="360"
                     Duration="0:0:1.1" />
  </Storyboard>

</ResourceDictionary>
